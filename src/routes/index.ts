import { router as authRouter } from "./auth.router"
import { router as itemRouter } from "./item.router"
import { router as roomRouter } from "./room.router"
import { router as shelfRouter } from "./shelf.router"
import { router as labelRouter } from "./label.router"
import {router as positionRouter} from "./position.router"
import {router as languageRouter} from "./language.router"
import {router as lightRouter} from "./light.router"
import {router as userRouter} from "./user.router"
import {router as settingsRouter} from "./settings.router"

export {authRouter, itemRouter, roomRouter, shelfRouter, labelRouter, positionRouter, languageRouter, lightRouter, userRouter, settingsRouter}
